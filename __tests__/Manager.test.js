const Manager = require('../lib/Manager.js');

test('checks to see if manager class passes', () => {
  const manager = new Manager("100");
    manager.officeNumber = officeNumber;

  expect(manager.officeNumber).toBe('100');
  
});